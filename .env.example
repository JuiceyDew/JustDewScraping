# --- Ollama Cloud ---------------------------------------------------------
# Get a key at https://ollama.com/settings/keys
OLLAMA_API_KEY=

# Model IDs as Ollama Cloud lists them (no "-cloud" suffix on /v1).
# See the live list with: uv run ideafindr doctor
OLLAMA_BASE_URL=https://ollama.com/v1
FAST_MODEL=gpt-oss:120b
SMART_MODEL=gpt-oss:120b

# --- Embeddings -----------------------------------------------------------
# auto = probe ollama cloud, then local ollama, then fall back to TF-IDF+SVD.
# Force one with: ollama-cloud | ollama-local | tfidf
# Cloud-only: Ollama Cloud serves no embeddings (/v1/embeddings 404,
# /api/embed 401), so clustering uses the in-process TF-IDF embedder.
# No local daemon, no container, no extra API key.
EMBED_BACKEND=tfidf
EMBED_MODEL=all-minilm
OLLAMA_LOCAL_URL=http://localhost:11434

# --- Optional scrapers (ToS-risky, off by default; see README) -------------
ENABLE_TIKTOK=false
ENABLE_INSTAGRAM=false
TIKTOK_MS_TOKEN=
INSTAGRAM_SESSION_USER=
